# Running Notes React Course

```shell
$ npm create vite@latest
Need to install the following packages:
create-vite@6.5.0
Ok to proceed? (y)


> npx
> "create-vite"

│
◇  Project name:
│  first-react
│
◇  Select a framework:
│  React
│
◇  Select a variant:
│  TypeScript
│
◇  Scaffolding project in /home/avilay/projects/github/learn-ui/learn-react/first-react...
│
└  Done. Now run:

cd first-react
npm install
npm run dev
```

